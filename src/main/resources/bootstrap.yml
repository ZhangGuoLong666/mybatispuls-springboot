spring:
  application:
    name: zgl-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        extension-configs:
          - data-id: zgl-auth.yml
            group: DEFAULT_GROUP
            refresh: true
            namespace: public
            server-addr: 127.0.0.1:8848